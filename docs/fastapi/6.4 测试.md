# 6.4 测试

测试是保证代码质量的重要手段。FastAPI 提供了便捷的测试支持。

## 测试客户端

### 安装依赖

```bash
pip install pytest httpx
```

### 基本测试

```python
# test_main.py
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)

def test_read_root():
 """测试根路由"""
 response = client.get("/")
 assert response.status_code == 200
 assert response.json() == {"message": "Hello World"}

def test_create_item():
 """测试创建商品"""
 response = client.post(
 "/items/",
 json={"name": "iPhone", "price": 5999}
 )
 assert response.status_code == 201
 data = response.json()
 assert data["name"] == "iPhone"
 assert data["price"] == 5999
```

## 测试路由

### GET 请求测试

```python
def test_list_items():
 """测试获取商品列表"""
 response = client.get("/items/")
 assert response.status_code == 200
 assert isinstance(response.json(), list)

def test_get_item():
 """测试获取单个商品"""
 response = client.get("/items/1")
 assert response.status_code == 200
 data = response.json()
 assert data["id"] == 1

def test_get_item_not_found():
 """测试商品不存在"""
 response = client.get("/items/999")
 assert response.status_code == 404
```

### POST 请求测试

```python
def test_create_item_success():
 """测试创建商品成功"""
 response = client.post(
 "/items/",
 json={
 "name": "MacBook",
 "price": 9999,
 "description": "笔记本电脑"
 }
 )
 assert response.status_code == 201
 data = response.json()
 assert data["name"] == "MacBook"
 assert "id" in data

def test_create_item_validation_error():
 """测试创建商品验证失败"""
 response = client.post(
 "/items/",
 json={"name": "MacBook"} # 缺少 price
 )
 assert response.status_code == 422
```

### PUT/DELETE 测试

```python
def test_update_item():
 """测试更新商品"""
 response = client.put(
 "/items/1",
 json={"price": 5499}
 )
 assert response.status_code == 200
 data = response.json()
 assert data["price"] == 5499

def test_delete_item():
 """测试删除商品"""
 response = client.delete("/items/1")
 assert response.status_code == 204
 
 # 验证已删除
 response = client.get("/items/1")
 assert response.status_code == 404
```

## 测试查询参数

```python
def test_query_parameters():
 """测试查询参数"""
 response = client.get(
 "/items/",
 params={
 "skip": 0,
 "limit": 5,
 "name": "iPhone"
 }
 )
 assert response.status_code == 200
```

## 测试请求头

```python
def test_with_headers():
 """测试请求头"""
 response = client.get(
 "/protected/",
 headers={"Authorization": "Bearer test-token"}
 )
 assert response.status_code == 200

def test_without_auth():
 """测试缺少认证"""
 response = client.get("/protected/")
 assert response.status_code == 401
```

## 测试 Cookie

```python
def test_with_cookies():
 """测试 Cookie"""
 response = client.get(
 "/me/",
 cookies={"session_id": "test-session"}
 )
 assert response.status_code == 200
```

## 测试文件上传

```python
def test_upload_file():
 """测试文件上传"""
 file_content = b"test file content"
 
 response = client.post(
 "/upload/",
 files={"file": ("test.txt", file_content, "text/plain")}
 )
 assert response.status_code == 200
 data = response.json()
 assert data["filename"] == "test.txt"
```

## 测试依赖覆盖

### 覆盖数据库依赖

```python
# test_main.py
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from database import Base, get_db
from main import app

# 测试数据库
SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(SQLALCHEMY_DATABASE_URL)
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

def override_get_db():
 """测试数据库依赖"""
 try:
 db = TestingSessionLocal()
 yield db
 finally:
 db.close()

# 覆盖依赖
app.dependency_overrides[get_db] = override_get_db

client = TestClient(app)

def setup_function():
 """每个测试前创建表"""
 Base.metadata.create_all(bind=engine)

def teardown_function():
 """每个测试后删除表"""
 Base.metadata.drop_all(bind=engine)

def test_create_item():
 """测试创建商品"""
 response = client.post(
 "/items/",
 json={"name": "Test Item", "price": 100}
 )
 assert response.status_code == 201
```

### 覆盖认证依赖

```python
from fastapi import Depends

def override_get_current_user():
 """模拟已认证用户"""
 return {"id": 1, "username": "testuser"}

app.dependency_overrides[get_current_user] = override_get_current_user

def test_protected_route():
 """测试受保护路由"""
 response = client.get("/users/me")
 assert response.status_code == 200
 assert response.json()["username"] == "testuser"
```

## 测试夹具

### 使用 pytest fixtures

```python
# conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from database import Base, get_db
from main import app

# 测试数据库
SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(SQLALCHEMY_DATABASE_URL)
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

@pytest.fixture
def db_session():
 """数据库会话夹具"""
 Base.metadata.create_all(bind=engine)
 session = TestingSessionLocal()
 try:
 yield session
 finally:
 session.close()
 Base.metadata.drop_all(bind=engine)

@pytest.fixture
def client(db_session):
 """测试客户端夹具"""
 def override_get_db():
 try:
 yield db_session
 finally:
 pass
 
 app.dependency_overrides[get_db] = override_get_db
 yield TestClient(app)
 app.dependency_overrides.clear()

@pytest.fixture
def auth_client(client):
 """带认证的测试客户端"""
 def override_get_current_user():
 return {"id": 1, "username": "testuser"}
 
 app.dependency_overrides[get_current_user] = override_get_current_user
 yield client
 app.dependency_overrides.clear()
```

### 使用夹具测试

```python
# test_items.py
def test_create_item(client):
 """测试创建商品"""
 response = client.post(
 "/items/",
 json={"name": "Test Item", "price": 100}
 )
 assert response.status_code == 201

def test_list_items(client):
 """测试获取商品列表"""
 # 先创建商品
 client.post("/items/", json={"name": "Item 1", "price": 100})
 client.post("/items/", json={"name": "Item 2", "price": 200})
 
 # 获取列表
 response = client.get("/items/")
 assert response.status_code == 200
 assert len(response.json()) == 2

def test_protected_route(auth_client):
 """测试受保护路由"""
 response = auth_client.get("/users/me")
 assert response.status_code == 200
```

## 异步测试

```python
# test_async.py
import pytest
import httpx
from main import app

@pytest.mark.asyncio
async def test_async_endpoint():
 """异步测试"""
 async with httpx.AsyncClient(app=app, base_url="http://test") as client:
 response = await client.get("/")
 assert response.status_code == 200
```

## 测试覆盖率

### 安装 coverage

```bash
pip install pytest-cov
```

### 运行测试并生成报告

```bash
# 运行测试并显示覆盖率
pytest --cov=app tests/

# 生成 HTML 报告
pytest --cov=app --cov-report=html tests/
```

## 完整示例

```python
# conftest.py
import pytest
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.database import Base, get_db
from app.main import app

SQLALCHEMY_DATABASE_URL = "sqlite:///./test.db"
engine = create_engine(SQLALCHEMY_DATABASE_URL)
TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)

@pytest.fixture(scope="function")
def db():
 Base.metadata.create_all(bind=engine)
 session = TestingSessionLocal()
 try:
 yield session
 finally:
 session.close()
 Base.metadata.drop_all(bind=engine)

@pytest.fixture(scope="function")
def client(db):
 def override_get_db():
 try:
 yield db
 finally:
 pass
 
 app.dependency_overrides[get_db] = override_get_db
 yield TestClient(app)
 app.dependency_overrides.clear()

# test_items.py
class TestItems:
 """商品测试类"""
 
 def test_create_item(self, client):
 """测试创建商品"""
 response = client.post(
 "/items/",
 json={"name": "iPhone", "price": 5999}
 )
 assert response.status_code == 201
 data = response.json()
 assert data["name"] == "iPhone"
 assert data["price"] == 5999
 
 def test_create_item_validation_error(self, client):
 """测试验证失败"""
 response = client.post(
 "/items/",
 json={"name": "iPhone"}
 )
 assert response.status_code == 422
 
 def test_list_items(self, client):
 """测试获取列表"""
 # 创建测试数据
 client.post("/items/", json={"name": "Item 1", "price": 100})
 client.post("/items/", json={"name": "Item 2", "price": 200})
 
 response = client.get("/items/")
 assert response.status_code == 200
 data = response.json()
 assert len(data) == 2
 
 def test_get_item(self, client):
 """测试获取单个商品"""
 # 创建商品
 create_response = client.post(
 "/items/",
 json={"name": "Test Item", "price": 100}
 )
 item_id = create_response.json()["id"]
 
 # 获取商品
 response = client.get(f"/items/{item_id}")
 assert response.status_code == 200
 assert response.json()["name"] == "Test Item"
 
 def test_get_item_not_found(self, client):
 """测试商品不存在"""
 response = client.get("/items/999")
 assert response.status_code == 404
 
 def test_update_item(self, client):
 """测试更新商品"""
 # 创建商品
 create_response = client.post(
 "/items/",
 json={"name": "Original", "price": 100}
 )
 item_id = create_response.json()["id"]
 
 # 更新商品
 response = client.put(
 f"/items/{item_id}",
 json={"name": "Updated", "price": 200}
 )
 assert response.status_code == 200
 data = response.json()
 assert data["name"] == "Updated"
 assert data["price"] == 200
 
 def test_delete_item(self, client):
 """测试删除商品"""
 # 创建商品
 create_response = client.post(
 "/items/",
 json={"name": "To Delete", "price": 100}
 )
 item_id = create_response.json()["id"]
 
 # 删除商品
 response = client.delete(f"/items/{item_id}")
 assert response.status_code == 204
 
 # 验证已删除
 response = client.get(f"/items/{item_id}")
 assert response.status_code == 404
```

## 小结

测试要点：

1. **TestClient**：使用 FastAPI 的测试客户端
2. **依赖覆盖**：使用 `dependency_overrides` 替换依赖
3. **测试夹具**：使用 pytest fixtures 管理测试资源
4. **覆盖率**：使用 pytest-cov 检测测试覆盖率
5. **异步测试**：使用 httpx.AsyncClient 测试异步代码

> **提示**：良好的测试覆盖可以大大提高代码质量和维护效率。
