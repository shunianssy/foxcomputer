# 4.3 Flask-WTF：更安全的表单

虽然可以用 request.form 拿数据，但为了防止 CSRF 攻击并进行数据校验（比如判断邮箱格式是否正确），我们会用 Flask-WTF

## 安装

```bash
pip install flask-wtf
```

## 配置

```python
app = Flask(__name__)
app.config['SECRET_KEY'] = 'your-secret-key'
app.config['WTF_CSRF_ENABLED'] = True  # 默认开启
```

## 定义表单类

```python
from flask_wtf import FlaskForm
from wtforms import StringField, PasswordField, TextAreaField, SelectField, BooleanField, SubmitField, IntegerField, DateField
from wtforms.validators import DataRequired, Email, Length, EqualTo, ValidationError

class RegistrationForm(FlaskForm):
    username = StringField('用户名', 
                          validators=[DataRequired(message='用户名不能为空'),
                                     Length(min=3, max=20, message='用户名长度应在3-20之间')])
    email = StringField('邮箱', 
                       validators=[DataRequired(message='邮箱不能为空'),
                                  Email(message='请输入有效的邮箱地址')])
    password = PasswordField('密码', 
                            validators=[DataRequired(message='密码不能为空'),
                                       Length(min=6, message='密码至少6位')])
    confirm_password = PasswordField('确认密码', 
                                    validators=[DataRequired(message='请确认密码'),
                                               EqualTo('password', message='两次密码不一致')])
    accept_terms = BooleanField('接受条款', 
                               validators=[DataRequired(message='必须接受条款')])
    submit = SubmitField('注册')
    
    def validate_username(self, field):
        """自定义验证器：检查用户名是否已存在"""
        if User.query.filter_by(username=field.data).first():
            raise ValidationError('用户名已被使用')
    
    def validate_email(self, field):
        """自定义验证器：检查邮箱是否已注册"""
        if User.query.filter_by(email=field.data).first():
            raise ValidationError('邮箱已被注册')
```

## 常用字段类型

| 字段类型 | 说明 | HTML 元素 |
|----------|------|-----------|
| StringField | 文本输入 | `<input type="text">` |
| PasswordField | 密码输入 | `<input type="password">` |
| TextAreaField | 多行文本 | `<textarea>` |
| IntegerField | 整数输入 | `<input type="number">` |
| FloatField | 浮点数输入 | `<input type="number">` |
| BooleanField | 复选框 | `<input type="checkbox">` |
| SelectField | 下拉选择 | `<select>` |
| SelectMultipleField | 多选下拉 | `<select multiple>` |
| RadioField | 单选按钮 | `<input type="radio">` |
| DateField | 日期选择 | `<input type="date">` |
| DateTimeField | 日期时间 | `<input type="datetime-local">` |
| FileField | 文件上传 | `<input type="file">` |
| SubmitField | 提交按钮 | `<button type="submit">` |

## 常用验证器

| 验证器 | 说明 |
|--------|------|
| `DataRequired()` | 必填字段 |
| `Email()` | 邮箱格式 |
| `Length(min, max)` | 长度限制 |
| `EqualTo(field)` | 与另一字段相等 |
| `NumberRange(min, max)` | 数值范围 |
| `URL()` | URL 格式 |
| `Regexp(pattern)` | 正则表达式 |
| `Optional()` | 可选字段 |
| `AnyOf(values)` | 值必须在列表中 |
| `NoneOf(values)` | 值不能在列表中 |

## 在视图中使用表单

```python
from flask import render_template, redirect, url_for, flash
from forms import RegistrationForm

@app.route('/register', methods=['GET', 'POST'])
def register():
    form = RegistrationForm()
    
    if form.validate_on_submit():
        # 表单验证通过
        user = User(
            username=form.username.data,
            email=form.email.data
        )
        user.set_password(form.password.data)
        db.session.add(user)
        db.session.commit()
        
        flash('注册成功！', 'success')
        return redirect(url_for('login'))
    
    # GET 请求或验证失败
    return render_template('register.html', form=form)
```

## 在 HTML 中渲染表单

### 基本渲染

```plaintext
<form method="POST">
    &#123;&#123; form.hidden_tag() &#125;&#125;
    
    <div class="form-group">
        &#123;&#123; form.username.label &#125;&#125;
        &#123;&#123; form.username(class="form-control", placeholder="请输入用户名") &#125;&#125;
        &#123;% for error in form.username.errors %&#125;
            <span class="error">&#123;&#123; error &#125;&#125;</span>
        &#123;% endfor %&#125;
    </div>
    
    <div class="form-group">
        &#123;&#123; form.email.label &#125;&#125;
        &#123;&#123; form.email(class="form-control") &#125;&#125;
        &#123;% for error in form.email.errors %&#125;
            <span class="error">&#123;&#123; error &#125;&#125;</span>
        &#123;% endfor %&#125;
    </div>
    
    <div class="form-group">
        &#123;&#123; form.password.label &#125;&#125;
        &#123;&#123; form.password(class="form-control") &#125;&#125;
    </div>
    
    <div class="form-group">
        &#123;&#123; form.confirm_password.label &#125;&#125;
        &#123;&#123; form.confirm_password(class="form-control") &#125;&#125;
    </div>
    
    <div class="form-check">
        &#123;&#123; form.accept_terms(class="form-check-input") &#125;&#125;
        &#123;&#123; form.accept_terms.label(class="form-check-label") &#125;&#125;
    </div>
    
    &#123;&#123; form.submit(class="btn btn-primary") &#125;&#125;
</form>
```

### 使用 Bootstrap 样式

```plaintext
<form method="POST" class="needs-validation" novalidate>
    &#123;&#123; form.hidden_tag() &#125;&#125;
    
    &#123;% for field in form %&#125;
        &#123;% if field.type not in ['CSRFTokenField', 'SubmitField'] %&#125;
        <div class="mb-3">
            &#123;&#123; field.label(class="form-label") &#125;&#125;
            &#123;% if field.type == 'BooleanField' %&#125;
                <div class="form-check">
                    &#123;&#123; field(class="form-check-input") &#125;&#125;
                    &#123;&#123; field.label(class="form-check-label") &#125;&#125;
                </div>
            &#123;% else %&#125;
                &#123;&#123; field(class="form-control" + (" is-invalid" if field.errors else "")) &#125;&#125;
                &#123;% for error in field.errors %&#125;
                    <div class="invalid-feedback">&#123;&#123; error &#125;&#125;</div>
                &#123;% endfor %&#125;
            &#123;% endif %&#125;
        </div>
        &#123;% endif %&#125;
    &#123;% endfor %&#125;
    
    &#123;&#123; form.submit(class="btn btn-primary") &#125;&#125;
</form>
```

## 文件上传表单

```python
from flask_wtf.file import FileField, FileAllowed, FileRequired

class UploadForm(FlaskForm):
    file = FileField('上传文件', 
                    validators=[
                        FileRequired(message='请选择文件'),
                        FileAllowed(['jpg', 'png', 'pdf'], message='只允许 jpg, png, pdf 格式')
                    ])
    submit = SubmitField('上传')

@app.route('/upload', methods=['GET', 'POST'])
def upload():
    form = UploadForm()
    if form.validate_on_submit():
        file = form.file.data
        filename = secure_filename(file.filename)
        file.save(os.path.join('uploads', filename))
        flash('上传成功！')
        return redirect(url_for('index'))
    return render_template('upload.html', form=form)
```

## CSRF 保护

### 启用 CSRF

```python
app.config['WTF_CSRF_ENABLED'] = True
app.config['WTF_CSRF_SECRET_KEY'] = 'your-csrf-secret-key'
```

### AJAX 请求中的 CSRF

```plaintext
<meta name="csrf-token" content="&#123;&#123; csrf_token() &#125;&#125;">

<script>
fetch('/api/endpoint', {
    method: 'POST',
    headers: {
        'X-CSRFToken': document.querySelector('meta[name="csrf-token"]').content
    },
    body: JSON.stringify(data)
});
</script>
```

### 禁用特定表单的 CSRF

```python
class APIForm(FlaskForm):
    class Meta:
        csrf = False
```

## 表单宏

创建可复用的表单宏：

```plaintext
<!-- macros/form.html -->
&#123;% macro render_field(field) %&#125;
<div class="form-group &#123;% if field.errors %&#125;has-error&#123;% endif %&#125;">
    &#123;&#123; field.label(class="form-label") &#125;&#125;
    &#123;&#123; field(class="form-control" + (" is-invalid" if field.errors else ""), **kwargs) &#125;&#125;
    &#123;% if field.errors %&#125;
        &#123;% for error in field.errors %&#125;
            <span class="invalid-feedback">&#123;&#123; error &#125;&#125;</span>
        &#123;% endfor %&#125;
    &#123;% elif field.description %&#125;
        <small class="form-text text-muted">&#123;&#123; field.description &#125;&#125;</small>
    &#123;% endif %&#125;
</div>
&#123;% endmacro %&#125;
```

使用宏：

```plaintext
&#123;% from "macros/form.html" import render_field %&#125;

<form method="POST">
    &#123;&#123; form.hidden_tag() &#125;&#125;
    &#123;&#123; render_field(form.username) &#125;&#125;
    &#123;&#123; render_field(form.email, placeholder="example@email.com") &#125;&#125;
    &#123;&#123; render_field(form.password) &#125;&#125;
    &#123;&#123; form.submit(class="btn btn-primary") &#125;&#125;
</form>
```

## 小结

| 功能 | 代码 |
|------|------|
| 定义表单 | `class MyForm(FlaskForm)` |
| 字段 | `StringField('标签', validators=[...])` |
| 验证 | `form.validate_on_submit()` |
| 获取数据 | `form.field.data` |
| 渲染字段 | <code>{{ form.field() }}</code> |
| 显示错误 | <code>{% for error in form.field.errors %}</code> |
| CSRF 令牌 | <code>{{ form.hidden_tag() }}</code> |

> 💡 **提示**：使用 Flask-WTF 可以自动处理 CSRF 保护，这是 Web 安全的基本要求。永远不要禁用 CSRF 保护，除非你有充分的理由。
