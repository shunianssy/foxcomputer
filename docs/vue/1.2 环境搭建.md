# 1.2 环境搭建

本节将介绍如何搭建 Vue.js 开发环境，包括 Node.js 安装、项目创建和开发工具配置。

## 环境要求

在开始之前，请确保你的系统已安装：

| 软件 | 版本要求 | 用途 |
|------|----------|------|
| Node.js | 18.0 或更高 | JavaScript 运行环境 |
| npm / pnpm | 最新版本 | 包管理器 |
| VS Code | 最新版本 | 代码编辑器（推荐） |

### 检查 Node.js 版本

```bash
node -v
# 应该显示 v18.0.0 或更高版本

npm -v
# 显示 npm 版本
```

## 安装 Node.js

### Windows 系统

1. 访问 [Node.js 官网](https://nodejs.org/)
2. 下载 LTS（长期支持）版本
3. 运行安装程序，按提示完成安装

### macOS 系统

使用 Homebrew 安装：

```bash
brew install node
```

### Linux 系统

使用包管理器安装：

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs
```

## 创建 Vue 项目

### 使用 create-vue（官方推荐）

```bash
# 创建新项目
npm create vue@latest my-vue-app

# 进入项目目录
cd my-vue-app

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### 项目创建选项

创建项目时，会提示选择以下选项：

```
✔ Add TypeScript? … No / Yes
✔ Add JSX Support? … No / Yes
✔ Add Vue Router for Single Page Application development? … No / Yes
✔ Add Pinia for state management? … No / Yes
✔ Add Vitest for Unit Testing? … No / Yes
✔ Add an End-to-End Testing Solution? › No
✔ Add ESLint for code quality? … No / Yes
✔ Add Prettier for code formatting? … No / Yes
```

根据项目需求选择相应功能。

### 使用 Vite 直接创建

```bash
# 使用 npm
npm create vite@latest my-vue-app -- --template vue

# 使用 pnpm
pnpm create vite my-vue-app --template vue

# 使用 yarn
yarn create vite my-vue-app --template vue
```

## 开发工具配置

### VS Code 扩展

推荐安装以下 VS Code 扩展：

| 扩展名称 | 用途 |
|----------|------|
| Vue - Official | Vue 语法高亮、智能提示 |
| ESLint | 代码质量检查 |
| Prettier | 代码格式化 |
| Volar | Vue 3 开发工具 |

### VS Code 配置

在项目根目录创建 `.vscode/settings.json`：

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "[vue]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

### 推荐扩展配置

创建 `.vscode/extensions.json`：

```json
{
  "recommendations": [
    "Vue.volar",
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode"
  ]
}
```

## 项目脚本命令

在 `package.json` 中定义的常用命令：

```json
{
  "scripts": {
    "dev": "vite",           // 启动开发服务器
    "build": "vite build",   // 构建生产版本
    "preview": "vite preview" // 预览生产构建
  }
}
```

运行命令：

```bash
# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 常见问题

### 1. npm 安装速度慢

使用国内镜像：

```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用 pnpm
npm install -g pnpm
pnpm config set registry https://registry.npmmirror.com
```

### 2. 端口被占用

修改 `vite.config.js`：

```javascript
export default defineConfig({
  server: {
    port: 3000  // 修改为其他端口
  }
})
```

### 3. Node 版本管理

推荐使用 nvm 管理 Node.js 版本：

```bash
# 安装 nvm (Windows 使用 nvm-windows)
# macOS/Linux
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 安装指定版本
nvm install 20

# 切换版本
nvm use 20
```

## 小结

本章学习了：

1. 安装 Node.js 和 npm
2. 使用 create-vue 创建 Vue 项目
3. 配置 VS Code 开发环境
4. 常用项目命令

> **提示**：良好的开发环境是高效编程的基础，花时间配置好环境会让后续的学习事半功倍。
