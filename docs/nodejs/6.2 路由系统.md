# 6.2 路由系统

Express 路由系统是框架的核心功能之一。本节将详细介绍路由的定义、组织和最佳实践。

## 路由方法

### HTTP 方法

```javascript
const express = require('express');
const app = express();

// 所有 HTTP 方法
app.get('/users', (req, res) => res.send('GET 用户列表'));
app.post('/users', (req, res) => res.send('POST 创建用户'));
app.put('/users/:id', (req, res) => res.send('PUT 更新用户'));
app.patch('/users/:id', (req, res) => res.send('PATCH 部分更新'));
app.delete('/users/:id', (req, res) => res.send('DELETE 删除用户'));

// 其他方法
app.head('/users', (req, res) => res.end());  // 只返回头
app.options('/users', (req, res) => {
    res.set('Allow', 'GET, POST, PUT, DELETE');
    res.end();
});

// 匹配所有方法
app.all('/catch-all', (req, res) => {
    res.send(`捕获 ${req.method} 请求`);
});
```

### 路由路径

```javascript
// 字符串路径
app.get('/about', (req, res) => res.send('关于页面'));

// 字符串模式
app.get('/ab?cd', (req, res) => res.send('ab?cd'));  // acd, abcd
app.get('/ab+cd', (req, res) => res.send('ab+cd'));  // abcd, abbcd, abbbcd
app.get('/ab*cd', (req, res) => res.send('ab*cd'));  // abcd, abRANDOMcd
app.get('/ab(cd)?e', (req, res) => res.send('abe 或 abcde'));

// 正则表达式
app.get(/a/, (req, res) => res.send('包含 a 的路径'));
app.get(/.*fly$/, (req, res) => res.send('以 fly 结尾'));
```

## 路由参数

### 路径参数

```javascript
// 单个参数
app.get('/users/:id', (req, res) => {
    console.log(req.params);  // { id: '123' }
    res.json({ userId: req.params.id });
});

// 多个参数
app.get('/users/:userId/posts/:postId', (req, res) => {
    const { userId, postId } = req.params;
    res.json({ userId, postId });
});

// 参数限制（正则）
app.get('/users/:id(\\d+)', (req, res) => {
    res.json({ id: parseInt(req.params.id) });
});

// 可选参数
app.get('/books/:year?/:month?', (req, res) => {
    const { year = 'all', month = 'all' } = req.params;
    res.json({ year, month });
});
```

### 查询参数

```javascript
app.get('/search', (req, res) => {
    const { q, page = 1, limit = 10, sort = 'created' } = req.query;
    
    res.json({
        query: q,
        pagination: {
            page: parseInt(page),
            limit: parseInt(limit)
        },
        sort
    });
});

// 多值查询参数
app.get('/filter', (req, res) => {
    // /filter?tags=node&tags=express
    const tags = req.query.tags;  // ['node', 'express'] 或 'node'
    
    // 确保是数组
    const tagArray = Array.isArray(tags) ? tags : [tags].filter(Boolean);
    
    res.json({ tags: tagArray });
});
```

### 请求体参数

```javascript
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

app.post('/users', (req, res) => {
    const { name, email, age } = req.body;
    
    // 验证
    if (!name || !email) {
        return res.status(400).json({ error: 'name 和 email 必填' });
    }
    
    res.status(201).json({
        id: Date.now(),
        name,
        email,
        age: age || null
    });
});
```

## 路由处理器

### 单个处理器

```javascript
app.get('/simple', (req, res) => {
    res.send('简单路由');
});
```

### 多个处理器

```javascript
// 多个回调函数
app.get('/multi', 
    (req, res, next) => {
        console.log('第一个处理器');
        req.customData = '传递数据';
        next();
    },
    (req, res) => {
        console.log('第二个处理器');
        res.send(`数据: ${req.customData}`);
    }
);

// 数组形式
const middleware1 = (req, res, next) => {
    console.log('中间件 1');
    next();
};

const middleware2 = (req, res, next) => {
    console.log('中间件 2');
    next();
};

app.get('/array', [middleware1, middleware2], (req, res) => {
    res.send('数组中间件');
});
```

## express.Router

### 创建路由模块

```javascript
// routes/users.js
const express = require('express');
const router = express.Router();

// 中间件（仅对此路由生效）
router.use((req, res, next) => {
    console.log('用户路由中间件');
    next();
});

// 获取用户列表
router.get('/', async (req, res) => {
    const users = await User.findAll();
    res.json(users);
});

// 获取单个用户
router.get('/:id', async (req, res) => {
    const user = await User.findById(req.params.id);
    if (!user) {
        return res.status(404).json({ error: '用户不存在' });
    }
    res.json(user);
});

// 创建用户
router.post('/', async (req, res) => {
    const user = await User.create(req.body);
    res.status(201).json(user);
});

// 更新用户
router.put('/:id', async (req, res) => {
    const user = await User.update(req.params.id, req.body);
    res.json(user);
});

// 删除用户
router.delete('/:id', async (req, res) => {
    await User.delete(req.params.id);
    res.status(204).send();
});

module.exports = router;
```

### 嵌套路由

```javascript
// routes/posts.js
const express = require('express');
const router = express.Router({ mergeParams: true });  // 合并父路由参数

router.get('/', async (req, res) => {
    const { userId } = req.params;  // 来自父路由
    const posts = await Post.findByUserId(userId);
    res.json(posts);
});

router.post('/', async (req, res) => {
    const { userId } = req.params;
    const post = await Post.create({ ...req.body, userId });
    res.status(201).json(post);
});

module.exports = router;

// app.js
const usersRouter = require('./routes/users');
const postsRouter = require('./routes/posts');

app.use('/users/:userId/posts', postsRouter);  // 嵌套路由
```

### 路由组织

```javascript
// routes/index.js
const express = require('express');
const router = express.Router();

const usersRouter = require('./users');
const postsRouter = require('./posts');
const commentsRouter = require('./comments');

router.use('/users', usersRouter);
router.use('/posts', postsRouter);
router.use('/comments', commentsRouter);

module.exports = router;

// app.js
const routes = require('./routes');
app.use('/api', routes);

// 最终路由：
// GET  /api/users
// POST /api/users
// GET  /api/users/:id
// GET  /api/posts
// ...
```

## 路由控制器

### 控制器模式

```javascript
// controllers/userController.js
class UserController {
    async getAll(req, res) {
        try {
            const users = await User.findAll();
            res.json(users);
        } catch (error) {
            res.status(500).json({ error: error.message });
        }
    }
    
    async getById(req, res) {
        try {
            const user = await User.findById(req.params.id);
            if (!user) {
                return res.status(404).json({ error: '用户不存在' });
            }
            res.json(user);
        } catch (error) {
            res.status(500).json({ error: error.message });
        }
    }
    
    async create(req, res) {
        try {
            const user = await User.create(req.body);
            res.status(201).json(user);
        } catch (error) {
            res.status(400).json({ error: error.message });
        }
    }
    
    async update(req, res) {
        try {
            const user = await User.update(req.params.id, req.body);
            res.json(user);
        } catch (error) {
            res.status(400).json({ error: error.message });
        }
    }
    
    async delete(req, res) {
        try {
            await User.delete(req.params.id);
            res.status(204).send();
        } catch (error) {
            res.status(500).json({ error: error.message });
        }
    }
}

module.exports = new UserController();

// routes/users.js
const express = require('express');
const router = express.Router();
const userController = require('../controllers/userController');

router.get('/', userController.getAll);
router.get('/:id', userController.getById);
router.post('/', userController.create);
router.put('/:id', userController.update);
router.delete('/:id', userController.delete);

module.exports = router;
```

## 路由中间件

### 认证中间件

```javascript
// middlewares/auth.js
const jwt = require('jsonwebtoken');

function authenticate(req, res, next) {
    const authHeader = req.headers.authorization;
    
    if (!authHeader?.startsWith('Bearer ')) {
        return res.status(401).json({ error: '未提供认证令牌' });
    }
    
    const token = authHeader.split(' ')[1];
    
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        req.user = decoded;
        next();
    } catch (error) {
        res.status(401).json({ error: '无效的令牌' });
    }
}

function authorize(...roles) {
    return (req, res, next) => {
        if (!roles.includes(req.user.role)) {
            return res.status(403).json({ error: '权限不足' });
        }
        next();
    };
}

module.exports = { authenticate, authorize };

// 使用
const { authenticate, authorize } = require('../middlewares/auth');

router.get('/profile', authenticate, userController.getProfile);
router.delete('/:id', authenticate, authorize('admin'), userController.delete);
```

### 验证中间件

```javascript
// middlewares/validate.js
const Joi = require('joi');

function validate(schema) {
    return (req, res, next) => {
        const { error, value } = schema.validate(req.body, {
            abortEarly: false,
            stripUnknown: true
        });
        
        if (error) {
            const errors = error.details.map(d => ({
                field: d.path.join('.'),
                message: d.message
            }));
            return res.status(400).json({ errors });
        }
        
        req.body = value;
        next();
    };
}

module.exports = validate;

// 使用
const userSchema = Joi.object({
    name: Joi.string().min(2).max(50).required(),
    email: Joi.string().email().required(),
    password: Joi.string().min(8).required()
});

router.post('/', validate(userSchema), userController.create);
```

## 小结

本节学习了：

1. **路由方法**：GET、POST、PUT、DELETE 等
2. **路由路径**：字符串、模式、正则表达式
3. **路由参数**：路径参数、查询参数、请求体
4. **路由处理器**：单个、多个、数组
5. **express.Router**：模块化路由
6. **嵌套路由**：路由层级结构
7. **控制器模式**：分离业务逻辑
8. **路由中间件**：认证、验证

> **提示**：良好的路由组织是项目可维护性的关键。推荐使用 express.Router 进行模块化，使用控制器分离业务逻辑。
