# 安装与配置

## Windows 安装 Docker

### 系统要求

- Windows 10 64-bit: Pro, Enterprise, or Education (Build 16299 or later)
- Windows 11 64-bit: Home or Pro version
- 启用 Hyper-V 和容器功能
- 至少 4GB 内存

### 安装步骤

1. 下载 Docker Desktop for Windows
   - 访问 [Docker 官网](https://www.docker.com/products/docker-desktop/)
   - 下载 Windows 版本的 Docker Desktop

2. 运行安装程序
   - 双击下载的安装程序
   - 按照安装向导完成安装
   - 安装完成后重启计算机

3. 启动 Docker Desktop
   - 从开始菜单启动 Docker Desktop
   - 等待 Docker 服务启动完成
   - 确认状态栏显示 Docker is running

4. 验证安装
   ```bash
   docker --version
   docker run hello-world
   ```

### 配置 Docker Desktop

1. 资源限制
   - 打开 Docker Desktop 设置
   - 转到 Resources
   - 调整内存、CPU 和磁盘空间限制

2. 镜像加速
   - 打开 Docker Engine 配置
   - 添加镜像加速器配置
   ```json
   {
     "registry-mirrors": [
       "https://docker.mirrors.ustc.edu.cn",
       "https://hub-mirror.c.163.com"
     ]
   }
   ```
   - 点击 Apply & Restart

## Linux 安装 Docker

### Ubuntu 安装

```bash
# 更新包索引
sudo apt-get update

# 安装依赖包
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# 添加 Docker 官方 GPG 密钥
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 设置 Docker 仓库
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker Engine
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 验证安装
sudo docker run hello-world
```

### CentOS 安装

```bash
# 安装依赖包
sudo yum install -y yum-utils

# 添加 Docker 仓库
sudo yum-config-manager \
    --add-repo \
    https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker Engine
sudo yum install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
sudo docker run hello-world
```

### 非 root 用户使用 Docker

```bash
# 创建 docker 用户组
sudo groupadd docker

# 将当前用户添加到 docker 组
sudo usermod -aG docker $USER

# 重新登录或运行以下命令使更改生效
newgrp docker

# 验证
docker run hello-world
```

## macOS 安装 Docker

### 系统要求

- macOS 11 或更高版本
- 至少 4GB 内存
- 支持的 Mac 芯片：Intel 或 Apple Silicon

### 安装步骤

1. 下载 Docker Desktop for Mac
   - 访问 [Docker 官网](https://www.docker.com/products/docker-desktop/)
   - 下载 Mac 版本的 Docker Desktop

2. 安装 Docker Desktop
   - 双击下载的 .dmg 文件
   - 将 Docker 拖动到 Applications 文件夹

3. 启动 Docker Desktop
   - 从 Applications 文件夹启动 Docker
   - 等待 Docker 服务启动完成

4. 验证安装
   ```bash
   docker --version
   docker run hello-world
   ```

## Docker 配置优化

### 镜像加速配置

#### Windows/macOS

在 Docker Desktop 设置中配置镜像加速器：
```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com",
    "https://mirror.ccs.tencentyun.com"
  ]
}
```

#### Linux

编辑 `/etc/docker/daemon.json` 文件：
```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

重启 Docker 服务：
```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 日志配置

配置容器日志大小限制，防止日志文件占用过多磁盘空间：
```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
```

### 存储驱动配置

根据系统选择合适的存储驱动：
- Linux: overlay2（推荐）
- Windows: windowsfilter
- macOS: overlay2

## Docker 常用命令

### 版本信息
```bash
docker --version
docker version
docker info
```

### 镜像相关
```bash
# 搜索镜像
docker search <镜像名>

# 拉取镜像
docker pull <镜像名>

# 查看本地镜像
docker images

# 删除镜像
docker rmi <镜像ID>
```

### 容器相关
```bash
# 运行容器
docker run <镜像名>

# 查看运行中的容器
docker ps

# 查看所有容器
docker ps -a

# 停止容器
docker stop <容器ID>

# 启动容器
docker start <容器ID>

# 删除容器
docker rm <容器ID>
```

## 故障排除

### Windows 问题

1. WSL 2 未安装
   - 下载并安装 [WSL2 Linux kernel update package](https://wslstorestorage.blob.core.windows.net/wslblob/wsl_update_x64.msi)

2. Hyper-V 未启用
   - 以管理员身份运行 PowerShell
   - 执行：`Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V -All`

### Linux 问题

1. 权限问题
   - 将用户添加到 docker 组
   - 或使用 sudo 运行 docker 命令

2. 服务未启动
   ```bash
   sudo systemctl start docker
   sudo systemctl enable docker
   ```

### macOS 问题

1. Docker Desktop 无法启动
   - 检查系统版本是否满足要求
   - 重新安装 Docker Desktop

2. 权限问题
   - 确保用户有访问 Docker 的权限
   - 检查安全设置

## 总结

本章介绍了在不同操作系统上安装和配置 Docker 的方法。Docker 的安装相对简单，但正确的配置对于后续的使用非常重要。特别是镜像加速和日志配置，可以显著提升使用体验。

在下一章中，我们将学习如何运行第一个 Docker 容器，并了解 Docker 的基本操作。
